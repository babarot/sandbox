name: Test

on:
  push:
    branches:
    - master

jobs:
  build-deploy:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v1

    - name: Test
      uses: b4b4r07/github-search-templater@master
      with:
        template: |
          hoge hoge
          hoge hoge
          hoge hoge
